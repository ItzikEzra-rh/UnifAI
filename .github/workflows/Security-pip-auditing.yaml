name: PIP Audit Dependency Security Scan

on: 
    push:
        branches:
            - GENIE-1354-dependency_scanning
    pull_request:
    workflow_dispatch:

jobs:

    # scan_backend:
    #     runs-on: ubuntu-latest
    #     steps:

    #     - name: Checkout code
    #       uses: actions/checkout@v4

    #     - name: Set up Python
    #       uses: actions/setup-python@v5
    #       with:
    #         python-version: '3.11'
    #         cache: 'pip'

    #     - name: setup virtual environment (backend)
    #       run: python3 -m venv venv
    
    #     - name: Install python requirements (backend)
    #       run: | 
    #         ./venv/bin/pip install --upgrade pip "setuptools>=70.0.0"
    #         ./venv/bin/pip install -r rag/requirements.txt

    #     - name: Install python requirements (backend)
    #       run: pip install -r backend/requirements.txt

    #     - name: Scan dependencies (backend)
    #       uses: pypa/gh-action-pip-audit@v1.1.0
    #       with:
    #         local: true
    #         virtual-environment: venv/
    #         summary: true

    scan_rag:
      runs-on: ubuntu-latest
      steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
        
      - name: setup virtual environment (rag)
        run: python3 -m venv venv

      - name: Install python requirements (rag)
        run: | 
          ./venv/bin/pip install --upgrade pip "setuptools>=70.0.0"
          ./venv/bin/pip install -r rag/requirements.txt

      - name: Scan dependencies (rag)
        uses: pypa/gh-action-pip-audit@v1.1.0
        with:
          local: true
          virtual-environment: venv/
          summary: true

    scan_multiagent:
      runs-on: ubuntu-latest
      steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
        
      - name: setup virtual environment (multiagent)
        run: python3 -m venv venv

      - name: Install python requirements (multiagent)
        run: | 
          ./venv/bin/pip install --upgrade pip "setuptools>=70.0.0"
          ./venv/bin/pip install -r multiagent/requirements.txt

      - name: Scan dependencies (multiagent)
        uses: pypa/gh-action-pip-audit@v1.1.0
        with:
          local: true
          virtual-environment: venv/
          summary: true

    scan_sso:
      runs-on: ubuntu-latest
      steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
        
      - name: setup virtual environment (sso)
        run: python3 -m venv venv

      - name: Install python requirements (sso)
        run: | 
          ./venv/bin/pip install --upgrade pip "setuptools>=70.0.0"
          ./venv/bin/pip install -r shared-resources/sso-backend/requirements.txt

      - name: Scan dependencies (sso)
        uses: pypa/gh-action-pip-audit@v1.1.0
        with:
          local: true
          virtual-environment: venv/
          summary: true

    analyze:
        needs: [scan_backend, scan_rag, scan_multiagent, scan_sso]
        if: always() && contains(needs.*.result, 'failure')
        runs-on: ubuntu-latest
        permissions:
            actions: read
            contents: read
        steps:

        - name: Generate Token
          id: generate_token
          #uses: tibdex/github-app-token@v2
          uses: actions/create-github-app-token@v2
          with:
            app-id: ${{ secrets.LOG_ANALYZER_APP_ID }}
            private-key: ${{ secrets.LOG_ANALYZER_PRIVATE_KEY }}

        - name: GitHub Actions Failure Analysis
          uses: calebevans/gha-failure-analysis@v0.1.9
          with:
            llm-provider: gemini
            llm-model: gemini-3-pro-preview
            llm-api-key: ${{ secrets.GEMINI_API_KEY }}
            github-token: ${{ steps.generate_token.outputs.token }}
            post-pr-comment: false