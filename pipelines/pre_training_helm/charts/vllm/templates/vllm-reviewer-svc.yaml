apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-reviewer-svc
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Chart.Name }}-reviewer
spec:
  type: {{.Values.service.type}}
  ports:
    - name: vllm-port
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ .Chart.Name }}-reviewer
