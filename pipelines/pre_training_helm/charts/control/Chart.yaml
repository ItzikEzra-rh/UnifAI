apiVersion: v2
name: control
description: Control chart will deploy 2 pods (Launchpad and Landing) as part of the dataset generation, which are responsible for transferring batches and uploading the final JSON to the repository.

type: application
version: 0.1.0
appVersion: "1.16.0"
dependsOn:
  - name: control
    version: "1.16.0"