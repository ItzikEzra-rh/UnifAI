apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "genie-prompt-lab.fullname" . }}-test-connection"
  labels:
    {{- include "genie-prompt-lab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "genie-prompt-lab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
