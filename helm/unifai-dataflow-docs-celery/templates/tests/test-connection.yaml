apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unifai-dataflow-celery-docs.fullname" . }}-test-connection"
  labels:
    {{- include "unifai-dataflow-celery-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unifai-dataflow-celery-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
