apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unifai-dataflow-celery-slack.fullname" . }}-test-connection"
  labels:
    {{- include "unifai-dataflow-celery-slack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unifai-dataflow-celery-slack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
