apiVersion: v1
kind: Service
metadata:
  name: {{ include "unifai-dataflow-celery-slack.fullname" . }}
  labels:
    {{- include "unifai-dataflow-celery-slack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unifai-dataflow-celery-slack.selectorLabels" . | nindent 4 }}
