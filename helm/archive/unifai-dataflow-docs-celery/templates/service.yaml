apiVersion: v1
kind: Service
metadata:
  name: {{ include "unifai-dataflow-celery-docs.fullname" . }}
  labels:
    {{- include "unifai-dataflow-celery-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unifai-dataflow-celery-docs.selectorLabels" . | nindent 4 }}
