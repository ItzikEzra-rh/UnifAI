apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "genie-reviewer.fullname" . }}-test-connection"
  labels:
    {{- include "genie-reviewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "genie-reviewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
