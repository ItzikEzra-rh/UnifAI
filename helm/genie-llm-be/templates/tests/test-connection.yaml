apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "genie-llm-be.fullname" . }}-test-connection"
  labels:
    {{- include "genie-llm-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "genie-llm-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
