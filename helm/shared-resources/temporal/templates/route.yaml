kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ include "temporal.fullname" . }}-route
  app: {{ .Chart.Name }}
  labels:
    paas.redhat.com/appcode: TAG-001
spec:
  host: 
  to:
    kind: Service
    name: {{ include "temporal.fullname" . }}-svc
    weight: 100
  port:
    targetPort: {{ .Values.service.port_ui }}
  wildcardPolicy: None