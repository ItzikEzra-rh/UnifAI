{
  "project_id": "MTA",
  "project_repo": "https://github.com/konveyor/tackle2-ui",
  "coding_language": "typescript",
  "system_message": "You are an expert in TypeScript, skilled in designing modular, scalable, and reusable code. You provide precise guidance on solving complex problems, ensuring optimal use of TypeScript features such as types, interfaces, generics, and modules. You follow clear and consistent coding principles, enabling clean, maintainable, and efficient codebases.",
  "context_template": "the {project_id} repository: {project_repo}.\nBelow is the element details:\n\n{element_type}\n\n{name}\n\n{file_location}\n\n{imports}\n\n{code}\n\nUtilize the provided element details and code to address the user's question accurately.",
  "element_templates": {
    "function": {
      "Function Understanding and Code Description": {
        "function_understanding_and_description": {
          "questions": [
            {
              "question": "What is the purpose of `{element_type}` `{name}` in the {project_id} project? Provide a detailed explanation of its responsibilities and significance within the project.",
              "validation": "Is the explanation comprehensive and clear about the purpose, responsibilities, and significance of `{element_type}` `{name}` within the project context?"
            },
            {
              "question": "Describe the key functionality of `{element_type}` `{name}` in the {project_id} project. How does it contribute to the overall project structure?",
              "validation": "Does the description effectively capture the key functionality and explain how `{element_type}` `{name}` contributes to the project's structure?"
            },
            {
              "question": "Explain the role of `{element_type}` `{name}` in the {project_id} project. What problem does it solve, and how does it integrate with other components?",
              "validation": "Is the explanation clear about the role, problem-solving approach, and integration of `{element_type}` `{name}` with other project components?"
            },
            {
              "question": "Provide an in-depth analysis of `{element_type}` `{name}` in the {project_id} project. Highlight its design considerations and key features.",
              "validation": "Does the analysis provide a thorough examination of the design considerations and key features of `{element_type}` `{name}`?"
            }
          ]
        },
        "function_usage_and_import": {
          "questions": [
            {
              "question": "Provide a code snippet demonstrating how to use `{element_type}` `{name}` in the {project_id} project, including the correct file location for importing it.",
              "validation": "Is the code snippet accurate in demonstrating the usage of `{element_type}` `{name}`, and does it include the correct import location?"
            },
            {
              "question": "Show an example of how to correctly import and use `{element_type}` `{name}` in the {project_id} project. Include the import path and a sample use case.",
              "validation": "Does the example clearly show the correct import method and provide a practical use case for `{element_type}` `{name}`?"
            },
            {
              "question": "How can `{element_type}` `{name}` be utilized within the {project_id} project? Provide a code example with proper imports from the correct file.",
              "validation": "Is the code example comprehensive in demonstrating the utilization of `{element_type}` `{name}`, with correct imports?"
            },
            {
              "question": "Write a code example that demonstrates the usage of `{element_type}` `{name}` in the {project_id} project, ensuring the import path is accurate.",
              "validation": "Does the code example accurately showcase the usage of `{element_type}` `{name}` with the correct import path?"
            }
          ]
        }
      },
      "Error Handling and Type Safety": {
        "error_handling_type_safety_options": {
          "questions": [
            {
              "question": "Demonstrate how `{element_type}` `{name}` uses TypeScript's type system to prevent runtime errors and improve code reliability within the {project_id} project, including specific type guards or narrowing techniques unique to the project.",
              "validation": "Does the demonstration effectively show the use of TypeScript's type system for error prevention, highlighting unique type guards or narrowing techniques?"
            },
            {
              "question": "Analyze the error handling mechanisms in `{element_type}` `{name}` within the {project_id} project, highlighting how TypeScript's strong typing contributes to robust error prevention and management.",
              "validation": "Is the analysis comprehensive in explaining how TypeScript's strong typing supports error prevention in `{element_type}` `{name}`?"
            },
            {
              "question": "Explain the strategies used in `{element_type}` `{name}` within the {project_id} project to ensure type safety, including the use of optional chaining, null checks, and type assertions specific to the project context.",
              "validation": "Does the explanation clearly outline the type safety strategies, including optional chaining, null checks, and type assertions?"
            },
            {
              "question": "Illustrate the implementation of comprehensive error handling patterns that leverage TypeScript's type system in `{element_type}` `{name}` within the {project_id} project.",
              "validation": "Is the illustration of error handling patterns thorough and effectively demonstrates the use of TypeScript's type system?"
            }
          ]
        }
      },
      "Performance and Type Optimization": {
        "performance_typing_options": {
          "questions": [
            {
              "question": "Identify opportunities to optimize type definitions for better compile-time type checking and runtime performance in `{element_type}` `{name}` within the {project_id} project.",
              "validation": "Are the optimization opportunities for type definitions comprehensive and specific to improving type checking and runtime performance?"
            },
            {
              "question": "Analyze the current type implementation in `{element_type}` `{name}` within the {project_id} project and suggest improvements for more efficient type checking and code execution.",
              "validation": "Does the analysis provide a detailed and actionable assessment of the current type implementation with clear improvement suggestions?"
            },
            {
              "question": "Demonstrate advanced type optimization techniques that could be applied to `{element_type}` `{name}` within the {project_id} project to enhance its performance and type safety.",
              "validation": "Are the demonstrated type optimization techniques advanced, practical, and likely to improve performance and type safety?"
            },
            {
              "question": "Explore how generic constraints and type narrowing can be used to improve the performance of `{element_type}` `{name}` in the {project_id} project.",
              "validation": "Does the exploration effectively show how generic constraints and type narrowing can optimize `{element_type}` `{name}`'s performance?"
            }
          ]
        }
      },
      "Modularity and Reusability": {
        "function_reusability_options": {
          "questions": [
            {
              "question": "Suggest improvements for modularity in `{element_type}` `{name}` within the {project_id} project by incorporating shared utilities and generic type patterns specific to the project.",
              "validation": "Are the suggested improvements for modularity comprehensive, practical, and aligned with the project's specific context?"
            },
            {
              "question": "Identify ways to extend `{element_type}` `{name}` in the {project_id} project for handling new conditions efficiently, using TypeScript's advanced type features unique to the project context.",
              "validation": "Do the identified extension methods demonstrate efficient handling of new conditions using advanced TypeScript type features?"
            },
            {
              "question": "Recommend refactoring opportunities in `{element_type}` `{name}` within the {project_id} project to enhance reusability through generics and interface design specific to the project.",
              "validation": "Are the recommended refactoring opportunities effective in improving reusability through generics and interface design?"
            },
            {
              "question": "Propose techniques to simplify `{element_type}` `{name}` by breaking down complex functions into smaller, reusable components with strong type definitions within the {project_id} project.",
              "validation": "Do the proposed techniques effectively simplify `{element_type}` `{name}` by creating more modular and reusable components?"
            }
          ]
        }
      }
    },
    "file": {
      "File Role and TypeScript Structure": {
        "file_role_structure_options": {
          "questions": [
            {
              "question": "Describe the purpose of `{element_type}` `{name}` in the {project_id} project, detailing how it organizes key functions and leverages TypeScript's modular structure specific to the {project_id} project.",
              "validation": "Is the description comprehensive in explaining the purpose, function organization, and TypeScript modular structure of `{element_type}` `{name}`?"
            },
            {
              "question": "Explain the `{element_type}` `{name}` within the {project_id} project, discussing its role in maintaining modularity, type safety, and testability within the project context.",
              "validation": "Does the explanation thoroughly cover the role of `{element_type}` `{name}` in maintaining modularity, type safety, and testability?"
            },
            {
              "question": "Provide a TypeScript code snippet that showcases the main functions in `{element_type}` `{name}`, illustrating its responsibilities in supporting the {project_id} project's type system.",
              "validation": "Is the code snippet effective in demonstrating the main functions and their responsibilities within the project's type system?"
            },
            {
              "question": "Generate a code snippet demonstrating essential functions and type interactions in `{element_type}` `{name}`, highlighting TypeScript-specific design patterns unique to the {project_id} project.",
              "validation": "Does the code snippet effectively highlight the unique TypeScript design patterns and type interactions in `{element_type}` `{name}`?"
            }
          ]
        }
      },
      "TypeScript Modularity and Optimization": {
        "file_modularity_refactoring_options": {
          "questions": [
            {
              "question": "Suggest ways to refactor and optimize `{element_type}` `{name}` in the {project_id} project to improve type safety, modularity, and performance specific to the project context.",
              "validation": "Are the suggested refactoring and optimization methods comprehensive and tailored to improving type safety, modularity, and performance?"
            },
            {
              "question": "Identify specific functions or type definitions in `{element_type}` `{name}` that could be modularized into more reusable and type-safe components within the {project_id} project.",
              "validation": "Do the identified functions and type definitions demonstrate clear potential for improved modularity and type safety?"
            },
            {
              "question": "Discuss opportunities to enhance type readability, simplify complex type structures, and optimize performance within `{element_type}` `{name}` in the {project_id} project.",
              "validation": "Is the discussion thorough in addressing type readability, type structure simplification, and performance optimization?"
            },
            {
              "question": "Recommend restructuring `{element_type}` `{name}` to reduce type complexity and increase type inference efficiency across multiple components in the {project_id} project.",
              "validation": "Are the recommendations effective in reducing type complexity and improving type inference efficiency?"
            }
          ]
        }
      }
    }
  }
}