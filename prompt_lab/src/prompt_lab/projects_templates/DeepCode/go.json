{
  "project_id": "oadp-operator",
  "project_repo": "github.com/openshift/oadp-operator",
  "coding_language": "go",
  "system_message": "You are an expert in Go programming, skilled in analyzing and improving modular, efficient, and maintainable code. You excel in understanding complex Go codebases, optimizing function and module usage, and ensuring adherence to best practices for performance, scalability, and security.",
  "context_template": "OADP is the OpenShift API for Data Protection operator. This open source operator sets up and installs Velero on the OpenShift platform, allowing users to backup and restore applications, Below is the element of type {element_type} with the name {name}:\n\n{file_location}\n\n{element_type}\n\n{tags}\n\n{package}\n\n{imports}\n\n{structs}\n\n{interfaces}\n\n{functions}\n\n{global_vars}\n\n{code}\n\nUtilize the provided element details and code to address the user's question accurately.",
  "element_templates": {
    "function": {
      "Repository Code": {
        "Repository structure": {
          "questions": [
            {
              "question": "Here is the {element_type} from the repository {project_repo}:\\n\\n** {element_type}: `{file_location}`**\\n```{coding_language}\\n/*\\n{imports}\\n*/\\n\\n{code}\\n\\n describe the {element_type} location within the repository.",
              "validation": "Is the location of the {element_type} in the repository accurately described?"
            }
          ]
        }
      },
      "Code Summary": {
        "Code purpose": {
          "questions": [
            {
              "question": "What is the purpose of `{element_type}` `{name}` in `{file_location}`? Provide a detailed explanation of its responsibilities and significance.",
              "validation": "Is the explanation comprehensive and clear about the purpose, responsibilities, and significance of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Exploring the key objectives of `{element_type}` `{name}` in `{file_location}`: Provide a detailed explanation of its responsibilities and importance.",
              "validation": "Is the explanation comprehensive and clear about the key objectives, responsibilities, and importance of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Analyzing the role of `{element_type}` `{name}` in `{file_location}`: Provide a detailed explanation of its responsibilities and relevance.",
              "validation": "Is the explanation comprehensive and clear about the role, responsibilities, and relevance of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "What is the significance of `{element_type}` `{name}` in `{file_location}`? Provide a detailed explanation of its purpose and responsibilities.",
              "validation": "Is the explanation comprehensive and clear about the significance, purpose, and responsibilities of `{element_type}` `{name}` in `{file_location}`?"
            }
          ]
        },
        "Code Logic": {
          "questions": [
            {
              "question": "What is the core logic implemented in `{element_type}` `{name}` in `{file_location}`? Provide a detailed explanation of its key operations.",
              "validation": "Is the explanation clear and accurate about the core logic and key operations of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Analyzing the decision-making process in `{element_type}` `{name}` in `{file_location}`: Provide a detailed explanation of its conditional logic and flow.",
              "validation": "Is the explanation comprehensive and clear about the decision-making process and conditional logic of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Exploring the algorithm in `{element_type}` `{name}` in `{file_location}`: Provide a detailed breakdown of the steps involved in its execution.",
              "validation": "Is the explanation clear and comprehensive about the algorithm and execution steps of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "What is the sequence of operations in `{element_type}` `{name}` in `{file_location}`? Provide a detailed summary of its logical flow.",
              "validation": "Is the explanation clear and accurate about the sequence of operations and logical flow of `{element_type}` `{name}` in `{file_location}`?"
            }
          ]
        }
      },
      "Code Dependencies": {
        "Calls": {
          "questions": [
            {
              "question": "What calls does `{element_type}` `{name}` in `{file_location}` make? List the calls and explain their purpose.",
              "validation": "Does the explanation  list the calls made by `{element_type}` `{name}` in `{file_location}` and clarify their purpose?"
            },
            {
              "question": "What is the purpose of the calls made by `{element_type}` `{name}` in `{file_location}`? List the calls and describe how they contribute to its functionality.",
              "validation": "Does the explanation list the calls made by `{element_type}` `{name}` in `{file_location}` and explain their contribution to its functionality?"
            },
            {
              "question": "What are the key function or method calls implemented in `{element_type}` `{name}` in `{file_location}`? List them and describe how they are executed.",
              "validation": "Does the explanation list the key function or method calls in `{element_type}` `{name}` in `{file_location}` and explain their execution?"
            },
            {
              "question": "How do the calls made by `{element_type}` `{name}` in `{file_location}` interact with other parts of the codebase? List the calls and describe their interactions.",
              "validation": "Does the explanation list the calls in `{element_type}` `{name}` in `{file_location}` and describe their interactions with the codebase?"
            },
            {
              "question": "What sequence of calls does `{element_type}` `{name}` in `{file_location}` execute? List the calls and explain how they relate to the function's behavior.",
              "validation": "Does the explanation list the sequence of calls in `{element_type}` `{name}` in `{file_location}` and clarify their relation to its behavior?"
            }
          ]
        },
        "Imports": {
          "questions": [
            {
              "question": "What imports does `{element_type}` `{name}` in `{file_location}` use? List the imports and explain their purpose.",
              "validation": "Does the explanation accurately list the imports used by `{element_type}` `{name}` in `{file_location}` and clarify their purpose?"
            },
            {
              "question": "How do the imports used in `{element_type}` `{name}` in `{file_location}` contribute to its functionality? List the imports and describe their roles.",
              "validation": "Does the explanation clearly list the imports in `{element_type}` `{name}` in `{file_location}` and explain their contribution to its functionality?"
            },
            {
              "question": "What are the specific imports required by `{element_type}` `{name}` in `{file_location}`? List them and describe how they are utilized.",
              "validation": "Does the explanation comprehensively list the specific imports required by `{element_type}` `{name}` in `{file_location}` and explain their usage?"
            },
            {
              "question": "What role do the imports in `{element_type}` `{name}` in `{file_location}` play in enabling its operations? List the imports and explain their impact.",
              "validation": "Does the explanation clearly list the imports in `{element_type}` `{name}` in `{file_location}` and describe their impact on the operations?"
            },
            {
              "question": "What dependencies are introduced through the imports in `{element_type}` `{name}` in `{file_location}`? List the imports and explain their significance.",
              "validation": "Does the explanation accurately list the dependencies introduced by the imports in `{element_type}` `{name}` in `{file_location}` and explain their significance?"
            }
          ],
          "condition": "{{ element.imports }}"
        },
        "Structs": {
          "questions": [
            {
              "question": "What structs does `{element_type}` `{name}` in `{file_location}` use? List the structs, show their code, and explain their purpose.",
              "validation": "Does the explanation accurately list the structs used by `{element_type}` `{name}` in `{file_location}`, show their structure, and clarify their purpose?"
            },
            {
              "question": "How do the structs used in `{element_type}` `{name}` in `{file_location}` contribute to its functionality? List the structs, show their code, and describe their roles.",
              "validation": "Does the explanation clearly list the structs in `{element_type}` `{name}` in `{file_location}`, include their structure, and explain their contribution to its functionality?"
            },
            {
              "question": "What is the structure or implementation of the structs used by `{element_type}` `{name}` in `{file_location}`? Provide the code of the structs and details about their fields and usage.",
              "validation": "Does the explanation accurately describe the structure or implementation of the structs used by `{element_type}` `{name}` in `{file_location}`, including their fields and usage?"
            },
            {
              "question": "How does `{element_type}` `{name}` in `{file_location}` interact with the structs it uses? Show the struct code and describe the interaction and its significance.",
              "validation": "Does the explanation clearly describe how `{element_type}` `{name}` in `{file_location}` interacts with the structs, show their code, and explain the significance of this interaction?"
            },
            {
              "question": "What role do the structs play in `{element_type}` `{name}` in `{file_location}`? Show the struct code and provide examples of how the structs are used within the function.",
              "validation": "Does the explanation provide clear examples of how the structs are used in `{element_type}` `{name}` in `{file_location}`, include their code, and explain their roles?"
            }
          ],
          "condition": "{{ element.structs }}"
        }
      },
      "Code Usage": {
        "Function Integration": {
          "questions": [
            {
              "question": "How can `{element_type}` `{name}` in `{file_location}` be correctly imported and used in another file? Provide an example.",
              "validation": "Does the explanation clearly describe how to import and use `{element_type}` `{name}` in `{file_location}` with a valid example?"
            },
            {
              "question": "What are the steps required to use `{element_type}` `{name}` in `{file_location}` in another context? Include the import statement and usage example.",
              "validation": "Does the explanation accurately detail the steps for importing and using `{element_type}` `{name}` in `{file_location}` with an example?"
            },
            {
              "question": "What is the correct way to import `{element_type}` `{name}` from `{file_location}` and call it with appropriate parameters? Provide an example.",
              "validation": "Does the explanation provide a clear and correct way to import and call `{element_type}` `{name}` from `{file_location}` with an example?"
            },
            {
              "question": "How should `{element_type}` `{name}` in `{file_location}` be used in a different module or package? Include a detailed explanation of the import and usage.",
              "validation": "Does the explanation include a clear import statement and usage details for `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "What is the proper syntax to import `{element_type}` `{name}` from `{file_location}` and utilize its functionality? Provide an example usage.",
              "validation": "Does the explanation provide the proper syntax to import `{element_type}` `{name}` from `{file_location}` and include an example of its usage?"
            }
          ]
        }
      },
      "Code Errors": {
        "Errors": {
          "questions": [
            {
              "question": "What errors or exceptions can `{element_type}` `{name}` in `{file_location}` produce? Provide a detailed explanation with examples.",
              "validation": "Does the explanation clearly identify the errors or exceptions that `{element_type}` `{name}` in `{file_location}` can produce, with examples?"
            },
            {
              "question": "How does `{element_type}` `{name}` in `{file_location}` handle errors or exceptions? Provide examples of its error-handling mechanisms.",
              "validation": "Does the explanation clearly describe how `{element_type}` `{name}` in `{file_location}` handles errors or exceptions, with examples?"
            },
            {
              "question": "What are the edge cases in `{element_type}` `{name}` in `{file_location}`, and how are errors handled for those cases?",
              "validation": "Does the explanation identify edge cases for `{element_type}` `{name}` in `{file_location}` and explain how errors are handled for those cases?"
            },
            {
              "question": "What error messages or codes can `{element_type}` `{name}` in `{file_location}` generate? Provide examples of when they occur.",
              "validation": "Does the explanation list the error messages or codes that `{element_type}` `{name}` in `{file_location}` can generate and provide examples of when they occur?"
            },
            {
              "question": "How can the error-handling behavior of `{element_type}` `{name}` in `{file_location}` be tested? Provide an example of testing error scenarios.",
              "validation": "Does the explanation provide clear steps to test the error-handling behavior of `{element_type}` `{name}` in `{file_location}`, with examples?"
            }
          ]
        },
        "Error Prevention": {
          "questions": [
            {
              "question": "What mechanisms does `{element_type}` `{name}` in `{file_location}` implement to prevent errors? Provide examples.",
              "validation": "Does the explanation clearly describe the mechanisms used in `{element_type}` `{name}` in `{file_location}` to prevent errors, with examples?"
            },
            {
              "question": "What checks or safeguards are implemented in `{element_type}` `{name}` in `{file_location}` to ensure error-free execution? Provide examples.",
              "validation": "Does the explanation clearly explain the checks or safeguards in `{element_type}` `{name}` in `{file_location}` to prevent errors, with examples?"
            },
            {
              "question": "How does `{element_type}` `{name}` in `{file_location}` handle edge cases proactively to prevent errors? Provide examples of edge case handling.",
              "validation": "Does the explanation describe how `{element_type}` `{name}` in `{file_location}` handles edge cases to prevent errors, with examples?"
            },
            {
              "question": "What strategies does `{element_type}` `{name}` in `{file_location}` use to detect potential errors before execution? Provide examples.",
              "validation": "Does the explanation describe the strategies used in `{element_type}` `{name}` in `{file_location}` to detect and prevent errors, with examples?"
            }
          ]
        }

      },
      "Code Security": {
        "Code Vulnerabilities": {
          "questions": [
            {
              "question": "What are the possible security risks in `{element_type}` `{name}` located at `{file_location}`? Provide examples and explanations.",
              "validation": "Does the explanation clearly identify the possible security risks in `{element_type}` `{name}` at `{file_location}` and provide examples?"
            },
            {
              "question": "What vulnerabilities might be present in `{element_type}` `{name}` at `{file_location}` that could be exploited? Provide examples and explanations.",
              "validation": "Does the explanation identify potential vulnerabilities in `{element_type}` `{name}` at `{file_location}` that could be exploited, with examples?"
            },
            {
              "question": "Are there any weaknesses in `{element_type}` `{name}` at `{file_location}` that could lead to security issues? Provide examples and detailed explanations.",
              "validation": "Does the explanation describe weaknesses in `{element_type}` `{name}` at `{file_location}` that could result in security issues, with examples?"
            },
            {
              "question": "What potential security flaws exist in `{element_type}` `{name}` located at `{file_location}`? Provide examples and an explanation of their impact.",
              "validation": "Does the explanation clearly identify potential security flaws in `{element_type}` `{name}` at `{file_location}` and explain their impact, with examples?"
            },
            {
              "question": "Could `{element_type}` `{name}` in `{file_location}` introduce security vulnerabilities? If so, what are they? Provide examples and explanations.",
              "validation": "Does the explanation identify whether `{element_type}` `{name}` at `{file_location}` could introduce security vulnerabilities, with examples and explanations?"
            }
          ]
        }

      }
    },
    "file": {
      "Repository Code": {
        "Repository structure": {
          "questions": [
            {
              "question": "Here is the {element_type} from the repository {project_repo}:\\n\\n** {element_type}: `{file_location}`**\\n```{coding_language}\\n\\n{code}\\n```\\n\\n describe the {element_type} location within the repository.",
              "validation": "Is the location of the {element_type} in the repository accurately described?"
            }
          ]
        }
      },
      "Code Summary": {
        "Code purpose": {
          "questions": [
            {
              "question": "What is the purpose of `{element_type}` `{name}` in `{file_location}`? Provide a detailed explanation of its responsibilities and significance.",
              "validation": "Is the explanation comprehensive and clear about the purpose, responsibilities, and significance of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Exploring the key objectives of `{element_type}` `{name}` in `{file_location}`: Provide a detailed explanation of its responsibilities and importance.",
              "validation": "Is the explanation comprehensive and clear about the key objectives, responsibilities, and importance of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Analyzing the role of `{element_type}` `{name}` in `{file_location}`: Provide a detailed explanation of its responsibilities and relevance.",
              "validation": "Is the explanation comprehensive and clear about the role, responsibilities, and relevance of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "What is the significance of `{element_type}` `{name}` in `{file_location}`? Provide a detailed explanation of its purpose and responsibilities.",
              "validation": "Is the explanation comprehensive and clear about the significance, purpose, and responsibilities of `{element_type}` `{name}` in `{file_location}`?"
            }
          ]
        },
        "Code Logic": {
          "questions": [
            {
              "question": "What is the core logic implemented in `{element_type}` `{name}` in `{file_location}`? Provide a detailed explanation of its key operations.",
              "validation": "Is the explanation clear and accurate about the core logic and key operations of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Analyzing the decision-making process in `{element_type}` `{name}` in `{file_location}`: Provide a detailed explanation of its conditional logic and flow.",
              "validation": "Is the explanation comprehensive and clear about the decision-making process and conditional logic of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "Exploring the algorithm in `{element_type}` `{name}` in `{file_location}`: Provide a detailed breakdown of the steps involved in its execution.",
              "validation": "Is the explanation clear and comprehensive about the algorithm and execution steps of `{element_type}` `{name}` in `{file_location}`?"
            },
            {
              "question": "What is the sequence of operations in `{element_type}` `{name}` in `{file_location}`? Provide a detailed summary of its logical flow.",
              "validation": "Is the explanation clear and accurate about the sequence of operations and logical flow of `{element_type}` `{name}` in `{file_location}`?"
            }
          ]
        }
      },
      "Code Dependencies": {
        "Functions": {
          "questions": [
            {
              "question": "What functions are used by `{element_type}` `{name}` in `{file_location}`? List the functions and explain their purpose.",
              "validation": "Does the explanation accurately list the functions used by `{element_type}` `{name}` in `{file_location}` and clarify their purpose?"
            },
            {
              "question": "How do the functions used in `{element_type}` `{name}` in `{file_location}` contribute to its functionality? List the functions and describe their roles.",
              "validation": "Does the explanation clearly list the functions in `{element_type}` `{name}` in `{file_location}` and explain their contribution to its functionality?"
            },
            {
              "question": "What specific functions are required by `{element_type}` `{name}` in `{file_location}`? List them and describe how they are utilized.",
              "validation": "Does the explanation comprehensively list the specific functions required by `{element_type}` `{name}` in `{file_location}` and explain their usage?"
            },
            {
              "question": "What role do the functions used in `{element_type}` `{name}` in `{file_location}` play in enabling its operations? List the functions and explain their impact.",
              "validation": "Does the explanation clearly list the functions in `{element_type}` `{name}` in `{file_location}` and describe their impact on the operations?"
            },
            {
              "question": "What dependencies are introduced through the functions used in `{element_type}` `{name}` in `{file_location}`? List the functions and explain their significance.",
              "validation": "Does the explanation accurately list the dependencies introduced by the functions in `{element_type}` `{name}` in `{file_location}` and explain their significance?"
            }
          ]
        },
        "Imports": {
          "questions": [
            {
              "question": "What imports are used in `{element_type}` `{name}` located at `{file_location}`? List the imports and explain their purpose.",
              "validation": "Does the explanation accurately list the imports used in `{element_type}` `{name}` at `{file_location}` and clarify their purpose?"
            },
            {
              "question": "How do the imports used in `{element_type}` `{name}` at `{file_location}` contribute to its functionality? List the imports and describe their roles.",
              "validation": "Does the explanation clearly list the imports in `{element_type}` `{name}` at `{file_location}` and explain their contribution to its functionality?"
            },
            {
              "question": "What specific imports are required by `{element_type}` `{name}` at `{file_location}`? List them and describe how they are utilized.",
              "validation": "Does the explanation comprehensively list the specific imports required by `{element_type}` `{name}` at `{file_location}` and explain their usage?"
            },
            {
              "question": "What role do the imports in `{element_type}` `{name}` at `{file_location}` play in enabling its operations? List the imports and explain their impact.",
              "validation": "Does the explanation clearly list the imports in `{element_type}` `{name}` at `{file_location}` and describe their impact on its operations?"
            },
            {
              "question": "What dependencies are introduced through the imports in `{element_type}` `{name}` at `{file_location}`? List the imports and explain their significance.",
              "validation": "Does the explanation accurately list the dependencies introduced by the imports in `{element_type}` `{name}` at `{file_location}` and explain their significance?"
            }
          ]
        }
      },
      "Code usage": {
        "Usage of Elements": {
          "questions": [
            {
              "question": "How can the elements in `{name}` located at `{file_location}` be correctly imported and used in another file? Provide an example for each element.",
              "validation": "Does the explanation clearly describe how to import and use the elements in `{name}` at `{file_location}` with examples for each?"
            },
            {
              "question": "What are the steps required to use the elements from `{name}` located at `{file_location}` in another file? Include the import statements and example usage.",
              "validation": "Does the explanation accurately detail the steps for importing and using the elements from `{name}` at `{file_location}` with clear examples?"
            },
            {
              "question": "What is the proper syntax to import the elements from `{name}` at `{file_location}` into another module or package? Provide an example usage for each element.",
              "validation": "Does the explanation provide the proper syntax to import the elements from `{name}` at `{file_location}` and include examples of their usage?"
            },
            {
              "question": "How should the elements in `{name}` located at `{file_location}` be integrated into a different project or file? Include a detailed explanation of the import and example usage.",
              "validation": "Does the explanation clearly describe how to integrate the elements in `{name}` at `{file_location}` into another project or file with examples?"
            },
            {
              "question": "What dependencies or imports are required to use the elements in `{name}` located at `{file_location}` elsewhere? Provide the import statements and an example for each element.",
              "validation": "Does the explanation clearly list the dependencies or imports needed for the elements in `{name}` at `{file_location}` and include examples of their usage?"
            }
          ]
        }
      },
      "Code Security": {
        "Code Vulnerabilities": {
          "questions": [
            {
              "question": "What are the possible security risks in `{element_type}` `{name}` located at `{file_location}`? Provide examples and explanations.",
              "validation": "Does the explanation clearly identify the possible security risks in `{element_type}` `{name}` at `{file_location}` and provide examples?"
            },
            {
              "question": "What vulnerabilities might be present in `{element_type}` `{name}` at `{file_location}` that could be exploited? Provide examples and explanations.",
              "validation": "Does the explanation identify potential vulnerabilities in `{element_type}` `{name}` at `{file_location}` that could be exploited, with examples?"
            },
            {
              "question": "Are there any weaknesses in `{element_type}` `{name}` at `{file_location}` that could lead to security issues? Provide examples and detailed explanations.",
              "validation": "Does the explanation describe weaknesses in `{element_type}` `{name}` at `{file_location}` that could result in security issues, with examples?"
            },
            {
              "question": "What potential security flaws exist in `{element_type}` `{name}` located at `{file_location}`? Provide examples and an explanation of their impact.",
              "validation": "Does the explanation clearly identify potential security flaws in `{element_type}` `{name}` at `{file_location}` and explain their impact, with examples?"
            },
            {
              "question": "Could `{element_type}` `{name}` in `{file_location}` introduce security vulnerabilities? If so, what are they? Provide examples and explanations.",
              "validation": "Does the explanation identify whether `{element_type}` `{name}` at `{file_location}` could introduce security vulnerabilities, with examples and explanations?"
            }
          ]
        }

      },
      "Code Errors": {
        "Errors": {
          "questions": [
            {
              "question": "What errors or exceptions can `{element_type}` `{name}` in `{file_location}` produce? Provide a detailed explanation with examples.",
              "validation": "Does the explanation clearly identify the errors or exceptions that `{element_type}` `{name}` in `{file_location}` can produce, with examples?"
            },
            {
              "question": "How does `{element_type}` `{name}` in `{file_location}` handle errors or exceptions? Provide examples of its error-handling mechanisms.",
              "validation": "Does the explanation clearly describe how `{element_type}` `{name}` in `{file_location}` handles errors or exceptions, with examples?"
            },
            {
              "question": "What are the edge cases in `{element_type}` `{name}` in `{file_location}`, and how are errors handled for those cases?",
              "validation": "Does the explanation identify edge cases for `{element_type}` `{name}` in `{file_location}` and explain how errors are handled for those cases?"
            },
            {
              "question": "What error messages or codes can `{element_type}` `{name}` in `{file_location}` generate? Provide examples of when they occur.",
              "validation": "Does the explanation list the error messages or codes that `{element_type}` `{name}` in `{file_location}` can generate and provide examples of when they occur?"
            },
            {
              "question": "How can the error-handling behavior of `{element_type}` `{name}` in `{file_location}` be tested? Provide an example of testing error scenarios.",
              "validation": "Does the explanation provide clear steps to test the error-handling behavior of `{element_type}` `{name}` in `{file_location}`, with examples?"
            }
          ]
        },
        "Error Prevention": {
          "questions": [
            {
              "question": "What mechanisms does `{element_type}` `{name}` in `{file_location}` implement to prevent errors? Provide examples.",
              "validation": "Does the explanation clearly describe the mechanisms used in `{element_type}` `{name}` in `{file_location}` to prevent errors, with examples?"
            },
            {
              "question": "What checks or safeguards are implemented in `{element_type}` `{name}` in `{file_location}` to ensure error-free execution? Provide examples.",
              "validation": "Does the explanation clearly explain the checks or safeguards in `{element_type}` `{name}` in `{file_location}` to prevent errors, with examples?"
            },
            {
              "question": "How does `{element_type}` `{name}` in `{file_location}` handle edge cases proactively to prevent errors? Provide examples of edge case handling.",
              "validation": "Does the explanation describe how `{element_type}` `{name}` in `{file_location}` handles edge cases to prevent errors, with examples?"
            },
            {
              "question": "What strategies does `{element_type}` `{name}` in `{file_location}` use to detect potential errors before execution? Provide examples.",
              "validation": "Does the explanation describe the strategies used in `{element_type}` `{name}` in `{file_location}` to detect and prevent errors, with examples?"
            }
          ]
        }

      }
    },
    "package": {
      "Repository Code": {
        "Repository structure": {
          "questions": [
            {
              "question": "Here is the {element_type} from the repository {project_repo}:\\n\\n** {element_type}: `{file_location}`**\\n{structs}\\n\\n{interfaces}\\n\\n{functions}  \\n describe the {element_type} location within the repository.",
              "validation": "Is the location of the {element_type} in the repository accurately described?"
            }
          ]
        }
      },
      "Code Dependencies": {
        "elements": {
          "questions": [
            {
              "question": "What are the dependencies of the package `{name}` in `{file_location}`? List and explain them.",
              "validation": "Does the explanation clearly list and describe the dependencies of the package `{name}` in `{file_location}`?"
            },
            {
              "question": "What external dependencies does the package `{name}` in `{file_location}` rely on? Provide a brief explanation.",
              "validation": "Does the explanation accurately identify and explain the external dependencies of the package `{name}` in `{file_location}`?"
            },
            {
              "question": "What modules or imports does the package `{name}` in `{file_location}` use? List and briefly describe their purpose.",
              "validation": "Does the explanation clearly list the modules or imports used by the package `{name}` in `{file_location}` and describe their purpose?"
            },
            {
              "question": "What are the key dependencies of the package `{name}` in `{file_location}`? Provide a simple list and explanation.",
              "validation": "Does the explanation clearly identify the key dependencies of the package `{name}` in `{file_location}` and provide a brief explanation?"
            },
            {
              "question": "What dependencies does the package `{name}` in `{file_location}` interact with? List them and explain their roles.",
              "validation": "Does the explanation clearly list the dependencies that the package `{name}` in `{file_location}` interacts with and explain their roles?"
            }
          ],
          "condition": "{{ element.structs or element.interfaces or element.functions }}"
        }
      },
      "Code usage": {
        "Usage of Package Elements": {
          "questions": [
            {
              "question": "How can the elements in the package `{name}` located at `{file_location}` be imported and used in another file or module? Provide examples of the import statements and usage.",
              "validation": "Does the explanation clearly describe how to import the elements in the package `{name}` at `{file_location}` and provide examples of their usage?"
            },
            {
              "question": "What is the correct way to import the elements from the package `{name}` in `{file_location}` and use them elsewhere? Provide examples of the syntax and usage.",
              "validation": "Does the explanation clearly explain the correct way to import and use the elements in the package `{name}` at `{file_location}` with examples?"
            },
            {
              "question": "How can the elements in `{name}` at `{file_location}` be utilized in another context or project? Include the import statements and usage examples.",
              "validation": "Does the explanation provide clear steps to import and use the elements in `{name}` at `{file_location}` in another context with examples?"
            },
            {
              "question": "What are the steps to import and use the elements in the package `{name}` at `{file_location}` from other files or modules? Provide examples.",
              "validation": "Does the explanation provide detailed steps to import and use the elements in `{name}` at `{file_location}` and include examples?"
            },
            {
              "question": "How can the elements defined in the package `{name}` at `{file_location}` be integrated into other parts of a codebase? Provide import statements and examples of their usage.",
              "validation": "Does the explanation describe how to integrate the elements from `{name}` at `{file_location}` into other parts of a codebase and include usage examples?"
            }
          ],
          "condition": "{{ element.structs or element.interfaces or element.functions }}"
        }
      }
    }
  }
}
